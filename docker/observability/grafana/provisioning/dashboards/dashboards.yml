apiVersion: 1

providers:
  - name: spring-obs-starter
    folder: Spring Observability
    type: file
    disableDeletion: true
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards
